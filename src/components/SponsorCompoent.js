import React from "react";
import { StyleSheet, View } from "react-native";

function SponsorCompoent(props) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default SponsorCompoent;
